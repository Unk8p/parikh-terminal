# Parikh Acquisition Terminal

Interactive dental practice acquisition dashboard. Static HTML/React app deployed to DigitalOcean App Platform.

## Run locally
Open `Terminal.html` in any browser.

## Deploy
Config is in `.do/app.yaml`. DO App Platform detects it automatically.
